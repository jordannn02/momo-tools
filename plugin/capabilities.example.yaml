- name: momo-tools
  type: plugin
  trigger:
    - capability router
    - tool routing
    - which tool
    - dashboard
    - audit tools
  scope: local capability index
  path_or_tool: plugin/scripts/momo-tools
  risk:
    - read-only
  source: public example
  owner: local user
  version: 0.2.0-public
  last_verified: 2026-07-08
  verification_level: verified-working
  known_caveats:
    - Example index only. Replace with your own local capabilities.
  canonical_for:
    - capability routing
    - risk gates
    - verification dashboard
  workflow_shape: index -> route -> gates -> evidence

- name: document-reader
  type: skill
  trigger:
    - pdf
    - docx
    - spreadsheet
    - document
    - summarize file
    - extract table
  scope: local documents
  path_or_tool: skills/document-reader/SKILL.md
  risk:
    - read-only
    - local-file
  source: example
  owner: local user
  version: example
  last_verified: not-verified
  verification_level: visible
  known_caveats:
    - Does not prove parser availability or OCR quality.
  canonical_for:
    - document analysis
    - file summary
  workflow_shape: convert -> inspect -> summarize

- name: browser-research
  type: connector
  trigger:
    - latest
    - current
    - today
    - website
    - browse
    - web research
    - verify online
  scope: external web
  path_or_tool: browser/search tool
  risk:
    - external-network
  source: example
  owner: local user
  version: example
  last_verified: not-verified
  verification_level: visible
  known_caveats:
    - Should not be used when the user explicitly says not to browse.
  canonical_for:
    - current information
    - official source lookup
  workflow_shape: search -> open primary sources -> cite
